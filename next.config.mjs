/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "cdn-icons-png.flaticon.com",
            "images.unsplash.com",
            "c5.patreon.com",
        ],
    },
};

export default nextConfig;
